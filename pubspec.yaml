name: adbutil
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  global_repository:
    path: /Users/nightmare/Desktop/nightmare-space/global_repository

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
